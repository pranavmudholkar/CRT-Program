#include <stdio.h>

int main()
{
    int a;
    printf("enter the value of a:");
    scanf("%d",&a);
    if (a%2==0)
    {
        printf("\n %d is even",a);
    }
    if(n%2!=0)
    {
    	printf("\n %d is odd",a)
	}
    return 0;
}
